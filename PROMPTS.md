# example prompts

> Create a simple deployment diagram with some Azure microservice. Use batch-add-cells for all vertices and edges, and prefer array parameters over repeated single calls. Each Azure component should have square dimension.

> Generate an Azure web app diagram with Front Door, two services, and a shared container registry. Use batch-add-cells with temp_id for all nodes and edges, and use search-shapes with queries if you need to look up multiple icons.
