# Docker registry URL (e.g., docker.io/myusername, ghcr.io/myorg)
REGISTRY=docker.io/myusername

# Semantic version for image tags (e.g., 1.0.0)
IMAGE_VERSION=latest

# ─── MCP Server Configuration ──────────────────────────────────
# These can also be set via CLI flags (CLI takes precedence).

# Transport type: stdio, http, or stdio,http (default: stdio)
# CLI equivalent: --transport <type>
TRANSPORT=stdio

# HTTP server port (default: 8080)
# CLI equivalent: --http-port <number>
HTTP_PORT=8080

# Logger type: console or mcp_server (default: console)
LOGGER_TYPE=console

# Path to Azure public service icons XML library (auto-detected if unset)
# AZURE_ICON_LIBRARY_PATH=./assets/azure-public-service-icons/000 all azure public service icons.xml
